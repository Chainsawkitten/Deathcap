#include <string>
#include <vector>

namespace Animation {
    /// An animation loaded from a file.
    class AnimationClip {
        public:


            std::string name;
        private:

    };
}