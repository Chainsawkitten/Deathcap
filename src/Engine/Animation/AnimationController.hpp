#include <string>

namespace Animation {
    /// An animation loaded from a file.
    class AnimationController {
        public:
            void Load(const std::string& name);

        private:

    };
}